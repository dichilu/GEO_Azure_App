# 版本控管紀錄 (Changelog)

## [v1.24.13] - 2026-08-21
### 修復（導流連結網址欄位桌機版排版錯誤：被擠成跟選單同一排）
- **背景**：使用者截圖回報桌機版排版跑掉——「導流連結網址」輸入框被擠到跟RBU/生成內容/目標優化引擎那排選單同一排的最右邊，欄位變得很窄、文字被截斷、新增/移除按鈕幾乎貼在一起。
- **根因**：v1.24.12把該欄位移到選單群組跟執行按鈕之間時，三者變成同一個 `flex lg:flex-row lg:justify-between` 容器的3個直接子元素——桌機版（lg以上）這個容器會強制把直接子元素排成一排，導致選單、CTA欄位、執行按鈕被硬擠在同一行。手機版因為原本就是直接往下堆疊（flex-col），沒有出現同樣的問題。
- **修復**：新增一層只包住「選單群組＋執行按鈕」兩者的內層flex容器，維持原本正確的排版邏輯；「導流連結網址」欄位移到這層外面，變成獨立一整行，寬度自動撐滿卡片。
- **驗證**：本機模擬桌機（1280px）與手機（375px）兩種寬度，實測量測DOM座標——桌機版確認選單+按鈕維持原本同排、底部對齊，CTA欄位獨立一整行、寬度撐滿卡片（非文章類型時CTA正確隱藏、選單+按鈕排版不受影響）；手機版確認CTA欄位寬度、輸入框、新增/移除按鈕皆正常顯示、無擠壓截斷。

## [v1.24.12] - 2026-08-20
### 調整（導流連結網址欄位移到目標優化引擎選單下方）
- **背景**：使用者反饋「導流連結網址」欄位原本放在Section 1「目標網址」文字框下面，位置不合理——這個欄位是SEO/GEO文章專屬功能，應該跟內容類型/目標優化引擎那排選單放在一起，而不是混在通用的目標網址輸入區。
- **修法**：把該欄位從Section 1移到RBU／內容類型／目標優化引擎／生成策略選單那一整排的正下方、執行按鈕正上方，選「SEO/GEO文章」時才顯示在這個新位置，其餘顯示/隱藏邏輯不變。

## [v1.24.11] - 2026-08-20
### 新增（SEO/GEO文章導流連結，支援多個候選網址）＋修復（配圖風格太暗沉憂鬱）
- **導流連結功能**：
  - 選「SEO/GEO文章」時介面顯示「導流連結網址」欄位，預設1個輸入框，可按「新增連結」加開更多列（各自可移除）。
  - 每列只填網址，不用填任何說明文字——新增後端輕量端點 `/api/fetch-title`（單純fetch＋抓`<title>`標籤，不跑完整Apify爬蟲），為每個候選網址抓一次真實網頁標題。
  - AI收到「候選連結清單＋各自真實標題」後，依每篇文章實際主題挑選最相關的一個，用 `{{CTA:索引:錨點文字}}` 占位符格式在BODY自然嵌入剛好一次（只決定選哪個、錨點文字寫什麼、放哪裡，AI完全不會自己寫網址）；事後由前端把占位符換成真正的markdown連結，網址是系統直接代入使用者輸入的網址，避免AI打錯或編造網址字元。若AI意外沒放占位符，退而求其次在文章結尾補一句用第一個候選連結的導流句，確保連結一定會被嵌入。
  - 連結渲染樣式為粗體＋品牌色底色，`target="_blank"` 開新分頁並加 `rel="noopener noreferrer"`；畫面預覽與Word匯出共用同一套渲染邏輯（`simpleMarkdownToHtml`新增markdown連結語法`[文字](網址)`的解析支援）。
  - **驗證**：真實測試填入2個候選網址（1個正常、1個故意設不存在路徑測試容錯）——正常網址正確抓到真實標題，故障網址抓標題失敗但不中斷流程（候選清單保留但無標題）；4篇文章的AI皆正確避開「無標題可判斷」的候選，一致選用有真實標題的候選，每篇正確嵌入剛好1個粗體可點擊連結、網址完全正確無誤。
- **配圖風格修復**：構圖描述指令與生圖API固定指令都加上「預設明亮、乾淨、有活力的光線與色調，不要暗色/去飽和/憂鬱風格」的規則，回應使用者反饋「目前寫實照片太憂鬱、毫無生氣」；只有「痛點型」文章角度本來就該表達問題/挑戰時才允許稍微偏暗，但也不能過度壓抑。

## [v1.24.10] - 2026-08-20
### 新增（RBU選單新增「HQ」總部單位選項）
- 在RBU下拉選單最前面新增 `HQ` 選項，與其餘非ACN地區（ANA/AEU/ATW等）行為一致——選擇後目標優化引擎維持預設清單（Standard GEO/Gemini/Claude/ChatGPT/Perplexity），不觸發ACN專屬的中國引擎清單切換邏輯。

## [v1.24.9] - 2026-08-20
### 修復（重點配圖：禁止畫出鎖固/安裝動作，避免產品懸空/卡進機櫃/支架不合理的細節錯誤）
- **背景**：使用者附上4張實測生成的配圖，指出研華產品在畫面裡的擺放/掛上位置很不自然——有的像是懸空卡進伺服器機櫃裡，有的用不合理的螺絲/夾具鎖在牆壁或管線上。
- **根因**：跟先前線材那次是同一種問題——生圖模型畫「精確的鎖固/安裝」這種物理動作本來就容易出錯，先前的指令只說「不要硬掰產品鎖在牆上的老套畫面」（避免陳腔濫調），沒有明確禁止「鎖固/安裝」這個動作本身，AI仍偶爾會描述成鎖上、鎖螺絲、用支架固定，导致生圖模型畫出懸空、卡進其他設備裡等不合理細節。
- **修復**：沿用線材那次的兩層修法模式——構圖描述指令（PRODUCT優先順序規則）與 `callImageGenerationApi()` 送給生圖模型的固定指令，都新增明確規則：產品若入鏡只能描述成「放置/靠著」在桌面、層架、工作台、地面等平面上，不能描述成鎖上、鎖螺絲、用夾具固定在牆壁/管線/機台上。
- **驗證**：用使用者先前測試的同一個PLP網址重新跑一次4篇文章＋8張配圖。結果：8組構圖描述文字全數不含「mount/bolt/screw/clamp/bracket」等鎖固相關字眼，多張明確使用「resting on」（放在...上面）的描述；人工檢視3張實際生成結果（層架收納櫃、機台旁層板、工作桌面），研華產品皆自然放置在平面上，未再出現懸空、卡進機櫃、不合理支架等問題。

## [v1.24.8] - 2026-08-20
### 修復（重點配圖：PLP類別頁多產品時，配圖能對照文章內文實際提到的產品，不再固定用頁面第一個產品）
- **背景**：使用者傳入PLP類別頁網址（一頁列多款不同產品）測試，發現配圖永遠只出現頁面第一個產品，即使文章內文明明討論的是後面的其他產品。
- **根因**：`gatherAllSourceContent()` 原本的候選照片去重邏輯是「每個來源網址只留信心度最高的1張候選」（`seenSources`，依 `sourceLabel` 去重）——這個設計是為單一產品頁（PDP，一個網址=一款產品）避免同一產品的多張角度照重複入選；但PLP類別頁是「一個網址、列出多款不同產品」，套用同一條規則會讓整頁所有產品的照片只留下最先出現的那1張，其餘產品的照片全部被濾掉，AI從頭到尾都沒有其他產品可選。
- **修復**：新增 `extractProductIdentifier()`，優先從圖片網址的 `/productfile/PIS/{型號}/` 路徑段判斷產品型號，抓不到再退而求其次用說明文字開頭的型號字樣；去重邏輯改為依「產品型號」（`seenProducts`）而非來源網址，候選上限從5張提高到8張。同時強化 `REFERENCE PHOTO SELECTION` 指令，明確要求AI「找出文章BODY實際點名的那個型號」再對應到候選清單，並警告候選清單可能包含多款不同產品、不要預設選第一個。
- **驗證**：用使用者提供的真實PLP網址（Edge & Device Products類別頁，21款產品）實測。修復前：候選清單只留1張（頁面第一個WISE-4250-S260）。修復後：候選清單正確保留8張不同產品的照片；實際產出4篇文章時，配圖分別正確對應到4種不同產品——「NVIDIA推論系統深度解析」文章用了AIR-075與MIC-742這兩款推論系統，「有線vs無線I/O比較」文章用了WISE-4250-S260與EKI-1526這兩款I/O/序列伺服器產品（其中一張構圖描述甚至直接寫出「Advantech EKI-1526-NI1A serial device server」，型號完全對上），沒有特定產品主角的FAQ文章則正確兩張都不帶參考圖，未見亂綁不相關產品的情況。

## [v1.24.7] - 2026-08-19
### 修復（重點配圖：單張生圖失敗時自動重打一次，減少文章只剩1張圖的狀況）
- **背景**：使用者發現有時候一篇文章只顯示1張配圖，而不是設計上固定的2張。
- **根因**：每篇文章固定會請AI寫2段構圖描述、平行呼叫2次生圖API，但單次生圖呼叫本身完全沒有重試機制（`callImageGenerationApi()` 用一般 `fetch`，不是文字生成那邊用的 `fetchWithRetry`）——只要其中一次呼叫遇到暫時性錯誤（Gemini API暫時異常、安全過濾擋掉等），直接回傳null，那張圖就永久留空，不會補救。
- **修復**：把單次呼叫邏輯抽成 `callImageGenerationApiOnce()`，`callImageGenerationApi()` 改成第一次失敗才自動重打一次，兩次都失敗才留空（維持原本「失敗不中斷整篇文章生成流程」的設計）。
- **驗證**：故意在瀏覽器端 monkey-patch `fetch`，強制讓生圖API的第一次呼叫回傳500錯誤，模擬暫時性故障，再跑一次真實的「4篇文章＋8張配圖」流程。結果：生圖API總共被呼叫9次（8張正常＋1次因故意失敗觸發的補打），最終8張配圖全部產出、沒有留空，證實重打機制確實在第一次失敗時會自動補打第二次並成功接住。

## [v1.24.6] - 2026-08-19
### 修復（重點配圖：禁止畫出線材，避免接口/插頭畫錯的細節錯誤）
- **背景**：使用者回報實測生成的配圖裡，硬體上的線材/插頭常常畫錯——附圖顯示一台設備上明明是不同類型的接口（例如根本不是網路孔），AI卻在旁邊畫了看起來像網路線的線材插進去，等於是接口類型都認錯，不是畫面工整度的問題。
- **修法（分兩層，逐步加強）**：
  1. 第一輪：在構圖描述指令加一條「盡量不要畫線材插入接口」的建議規則——實測仍會生出一張線材末端懸空未插好的圖，效果不夠。
  2. 第二輪：改成「絕對禁止」語氣重測——構圖描述本身確實沒再提線材，但生圖模型在畫「產品掛載在機器人/機台內部」這類畫面時，自己聯想加了線材上去，光靠構圖描述層的文字擋不住這種生圖模型自行聯想的行為。
  3. 第三輪（依使用者確認方向）：把規則收斂成「乾脆完全不要畫線材」，且不只寫在構圖描述指令裡，同時加進每次呼叫生圖API時固定夾帶的指令文字（`callImageGenerationApi()`，直接送進Gemini圖片模型本身），確保就算構圖描述沒提到，生圖這一步本身也會被要求不要畫任何線材/電線/插頭觸碰或靠近研華產品；畫面裡明顯不是研華產品的背景元素（例如模糊的機櫃）不受此限制。
- **驗證**：三輪修改皆用真實API重新產出4篇文章＋8張配圖驗證。第三輪（無線材版本）實測：8張配圖的構圖描述文字全數不含「cable/wire/cord/plug」關鍵字，人工檢視其中3張生成結果（接口特寫圖、產品桌上特寫圖、AMR機器人掛載圖），前兩張完全乾淨無線材；AMR掛載圖仍在研華產品旁出現線材（生圖模型自行加上，構圖描述本身沒有要求），故追加第三層修法直接把禁令寫進生圖API固定指令，強化擋下這類生圖模型自行聯想的行為。

## [v1.24.5] - 2026-08-19
### 新增＋修復（重點配圖：帶入真實研華產品照片當生圖參考，並修好一個網址解析bug）
- **背景**：使用者提出希望文章配圖帶到硬體時，能用「真實的研華產品」而非AI憑空想像的機器；使用者明確拒絕手動上傳圖檔或讓AI自行上網搜圖，指定沿用現有介面已有的「目標網址」與「同產品線URL（Reference Data）」欄位——這兩處本來就會被爬取，只是沒把爬回內容裡的圖片網址利用起來。
- **新增**：新增 `extractProductPhotoCandidates()`，從爬蟲抓回的Markdown內容裡找出研華官網圖檔命名慣例含「Photo/Main/Product」字樣的圖片網址，彙整成候選清單（同一來源只留信心度最高1張，最多5張）；新增後端 `/api/fetch-image` 代理端點，把候選圖網址轉存成base64（避開瀏覽器直接抓外部圖片的CORS限制）；構圖prompt新增候選清單與選用規則，AI依畫面實際需要選擇對應候選索引或回答「none」；`/api/generate-image` 支援夾帶參考圖（連同一段「產品外觀忠實度」指令），讓生圖模型照真實外觀畫、不重新設計。
- **修復（真實bug，非設計瑕疵）**：第一版 `extractProductPhotoCandidates()` 用 `[^)]+` 擷取圖片網址，但研華官網圖檔網址常見跳脫括號寫法（例如 `Photo\(Main\)`），導致網址在跳脫過的 `)` 處就被提早截斷成不完整字串，通不過副檔名檢查而被整批濾掉——實測4篇文章8張生圖全部回答「none」，追查真相是候選清單根本是空的（AI沒東西可選，不是AI判斷不需要），並非設計預期的「非必要不強制」行為。改用 `(?:\\.|[^)])+` 讓跳脫字元整組被吃掉才修正。
- **驗證**：修復前後皆用真實API完整跑過一次「4篇文章＋8張配圖」流程比對——修復前0張圖片有候選清單、8張皆為none；修復後同一產品頁（AFE-R770）正確抓到1張候選產品照，8張配圖中5張使用該真實產品照當參考圖生成（技術/硬體特寫類畫面），3張未使用（畫面主體非硬體，如人物討論情境），行為符合設計預期；並直接把其中一張參考圖生成結果與原始真實產品照片人工比對，機殼散熱鰭片紋路、LAN/USB接口配置、COM埠、DIO接頭位置、機身紫色飾線皆一致，確認並非AI憑空捏造的通用工業電腦造型。

## [v1.24.4] - 2026-08-18
### 修復（文章內容編造：型號打錯字元、腦補不存在的規格變體）
- **背景**：使用者截圖回報生成文章出現「MIC-743-AT7A1」（正確應為742）與「T4000規格支援3個25GbE」這種看起來像編造的內容。
- **查證**：直接重新爬取產品原始網頁逐字比對，確認：型號從頭到尾都是「MIC-742-AT7A1」，從未出現743；原文規格寫的是「Supports 1 x QSFP28 (T5000: 4 x 25GbE)」，從未提過「T4000」或「3 x 25GbE」——兩者皆確認為AI編造，非誤讀截圖。
- **根因**：原文用括號寫「(T5000: 4 x 25GbE)」，這種寫法容易讓AI誤以為暗示「還有其他型號的不同配置」，進而自己腦補出不存在的T4000/3組25GbE版本；也不排除近幾輪新增的「多用產業通用知識」框架邏輯，讓AI在遇到模糊資訊時更容易腦補細節，而非老實只講原文那一個事實。
- **修復**：在防止編造規則裡明確加上兩條——①原文用括號等方式暗示可能有其他型號/規格版本時，不能自己延伸腦補不存在的版本，只能陳述原文明確給的那一個事實；②產品型號/料號這類代碼，必須逐字元完全比照原文複製，不能自行改動任何一個字元。
- 已用真實API重新測試驗證（繁體中文、同一產品）：4篇文章合計25次型號提及，全部正確為「MIC-742-AT7A1」，0次「743」；全文搜尋「T4000」與「3個25GbE」皆為0筆；QSFP28規格段落確認只陳述原文「NVIDIA Jetson T5000支援4個25GbE」這一個事實，未再腦補其他型號變體。

## [v1.24.3] - 2026-08-17
### 修復（文章段落字級不一致：標題與緊接內文中間沒空行時，內文漏包段落標籤）
- **背景**：使用者實測發現同一段落內文字級「上半段大、下半段小」，附截圖並框出實際位置。
- **根因（已用使用者截圖裡的真實文字重現確認）**：`simpleMarkdownToHtml()` 原本用「空行」判斷段落邊界，但如果AI生成的內文把「## 標題」跟緊接著的內文寫在同一段（中間沒有空行），這整塊（標題+後面所有內文）會被判斷成「以標題開頭」而整塊原封不動輸出——後面的內文完全沒被包進 `<p>` 標籤，變成裸文字，用瀏覽器預設字級顯示（比正常段落的14px大），直到下一個真的有空行分隔的段落才恢復正常，造成同一段落視覺上「一部分大、一部分小」。
- **修復**：在標題轉換完成後，強制在每個標題後面補一個空行，確保無論AI原始輸出有沒有空行分隔，內文都會被切成獨立區塊、正確包進段落標籤。
- **影響範圍**：`simpleMarkdownToHtml()` 為畫面預覽與Word匯出共用的函式，此修復同時套用於兩者。
- 已用使用者截圖裡的真實文字做隔離單元測試驗證：修復前，同樣輸入產生的HTML完全沒有任何`<p>`標籤（0個）；修復後正確產生2個`<p>`標籤，標題與內文分離清楚，字級統一。

## [v1.24.2] - 2026-08-17
### 修復（SEO/GEO文章：段落規格清單化、空話式產品連結、補充段文不對題）
- **背景**：v1.24.1上線「主題框架優先」邏輯後，使用者實測「買購指南」文章發現三個殘留問題：①連接性段落把所有規格一次列完，讀起來像規格表不像購買建議；②某段落連到產品時只寫空洞形容詞（例如「確保無縫整合」）沒有實際數字/機制佐證；③文章結尾常出現一個「補充/整合挑戰」類型的段落，標題承諾一個主題、內文卻只是把前面沒用到的規格硬塞進去，造成標題與內文對不上（文不對題）。
- **修復①規格清單化**：新增規則明確要求「連到產品時只引用1-2個跟該判斷點最相關的規格，不要把來源內容裡相關的規格全部倒出來」。
- **修復②空話式連結**：新增規則「如果來源內容對某個判斷點沒有具體佐證，不要硬掰一句空洞的產品主張，寧可誠實用產業通用說法帶過」。
- **修復③補充段文不對題**：新增規則「禁止為了塞剩餘規格而生出一個額外的『整合考量/其他特色』類段落，剩餘規格若跟已建立的判斷點無關，直接併入最相關的段落或乾脆不提，不要為了塞這些規格另開一個標題後撐不起內容的段落」。
- 已用真實API重新測試驗證：①改版後的「技術深度型」文章（QSFP28/GMSL2.0/CANbus連接技術）前4段皆確認先講技術重要性再接1-2個精簡具體規格，未再出現規格清單化；②同一次測試最後一段（可擴充性與系統整合）確認標題與內文一致，具體規格（PCIe Gen5八通道、M.2擴充槽）直接對應「可擴充性」主題，未再出現空洞的補充段落。

## [v1.24.1] - 2026-08-17
### 新增＋修復（重點配圖：AI標示浮水印、構圖邏輯改為主題優先）
- **背景**：使用者實測v1.24.0後回報「感受不到主題/段落內容對生成圖片的影響差異」。因為沒有任何地方保存生成過程的log，改用重新真實測試、直接擷取AI實際產出的構圖描述文字來查證，而不是憑印象回答。
- **查證發現的真實問題**：4篇文章的圖1（主視覺）幾乎都收斂成同一個「工廠+機械手臂+一台工業電腦」概念，圖2也都圍繞同一個規格（GMSL2.0/mini FAKRA接頭）——根本原因是4篇文章共用同一個產品，AI每次都反射性抓最好畫的1-2個規格當錨點，沒有真的依each篇文章自己的角度去想構圖。
- **新增AI標示浮水印**：沿用「AI Content Center」專案已驗證過的做法（該專案曾嘗試讓生圖模型自己把標示文字畫進場景，6輪調整都不準確後改為生圖後用程式碼疊固定PNG素材）——複製同一份PNG素材（`ai_modified_badge.png`）與同一套尺寸公式，在瀏覽器端用Canvas疊到每張生成圖的右下角。
- **構圖決策邏輯改版**：依使用者指示的優先順序重寫prompt——①先決定這篇文章自己的主題角度、②依主題決定畫面主體（不限定是產品，可以是人物/情境/流程）、③文章內文有無提到具體應用場景、④產品本身放最低優先、非必要。
- 已用真實API重新測試驗證：修改後4篇文章的圖1主體明確不同（購買指南→工程師採購人員討論情境；技術深度→控制面板特寫；FAQ→顧問式規劃討論情境；實務場景→AMR機器人導航倉庫），不再收斂成同一個工廠機械手臂畫面；AI標示浮水印在實際生成圖片右下角正確顯示。

## [v1.24.0] - 2026-08-14
### 新增（SEO/GEO文章：每篇自動生成2張1200×628重點配圖，畫面與Word匯出同步顯示）
- **圖片來源**：沿用現有 `GEMINI_API_KEY`，新增後端代理 `/api/generate-image` 呼叫 Gemini 原生圖片模型（gemini-2.5-flash-image，跟先前像素風格小人物用的是同一顆模型），不需申請新金鑰。
- **構圖決定方式**：文章生成的同一次AI呼叫裡，多請AI依實際寫出的內文，額外產出2段構圖描述（寫實攝影風格、硬體應用場景，不含文字/Logo）——第1張是文章整體主題的主視覺，第2張取材自內文提到的1個具體技術/場景細節，不額外多花一次AI呼叫。
- **尺寸處理**：Gemini生成的圖片尺寸不固定，改用瀏覽器端Canvas做「置中裁切縮放」（cover模式，不變形拉伸），確保輸出精確為1200×628。
- **套用範圍**：畫面預覽（結果卡片內嵌顯示）與Word匯出（以base64圖片內嵌）同步支援。
- **容錯設計**：任一張圖片生成失敗時，該張圖片留空，不中斷整體文章生成流程。
- 已用真實API完整測試驗證：4篇文章共8次圖片生成請求，7次成功（1次因API呼叫機率性失敗，已確認為優雅降級、未影響其他內容）；成功的7張圖片皆確認精準裁切為1200×628像素；**用電腦本機真正安裝的 Microsoft Word（PowerShell COM自動化）實際開啟匯出檔案**，確認7張圖片皆正確嵌入為真實的Word圖片物件（非僅程式碼層級檢查）。
- **誠實記錄一個副作用**：內嵌圖片會讓Word匯出檔案體積明顯變大（這次測試4篇文章8張圖片，匯出的HTML內容約14MB），屬於預期中的正常現象，非bug。

## [v1.23.4] - 2026-08-14
### 改版（目標優化引擎：ACN也加入標準GEO選項，全地區項目名稱SEO改為GEO）
- **ACN（中國）清單新增第一項「Standard GEO」**：原本ACN清單只有文心一言/通義千問/DeepSeek/豆包4項，現在跟其他地區一樣，第一項也有「標準GEO優化」可選。
- **所有地區的原「Standard SEO」項目名稱統一改為「Standard GEO」**：5語言介面文字（繁中/簡中/英/日/韓）與頁面預設HTML同步更新，內部資料值仍維持`Standard SEO`不變（避免牽動其他邏輯判斷）。
- **修正切換語言時的更新邏輯**：原本語言切換只在非ACN狀態才更新這個選項的文字（因為以前ACN第一項是專有名詞不用翻譯），現在ACN也有這個共用選項，改為兩種狀態都更新。
- 已本機測試驗證：預設狀態、切ACN、切回其他地區、ACN狀態下切換5種介面語言，「標準GEO優化」文字皆正確顯示對應語言版本。

## [v1.23.3] - 2026-08-13
### 修復（介面版號從未跟著更新、清除一處誤植的舊版號標籤）
- **背景**：這次session多次修改並升版（v1.22.4~v1.23.2），每次都有更新`package.json`/`README.md`/`CHANGELOG.md`的版號，但唯獨網頁介面實際顯示給使用者看的版號（分頁標題與導覽列文字）從頭到尾寫死在`v1.21.3`，從未同步更新——導致使用者完全看不到任何「新版本真的上線了」的肉眼可見證據。
- **修復**：分頁標題、5語言導覽列版號文字，全部同步更新為`v1.23.3`。
- **順手清掉一個無關的舊版號殘留**：產品文案結果卡片的標題旁邊，原本寫死顯示`(v1.18.3)`（型號名稱後面直接接一個系統版號，跟產品內容無關，應該是先前開發時的殘留），予以移除。
- 已本機測試確認：分頁標題與5語言導覽列版號皆正確顯示為`v1.23.3`。

## [v1.23.2] - 2026-08-13
### 修復（真正修好：SEO/GEO文章匯出Word「第一篇之後整個大空白、一行字一頁」）
- **背景**：v1.22.4宣稱修好Word匯出排版，實際上線後使用者回報還是壞的——第一篇之後每頁只有一行字、其餘整頁空白。v1.22.4當時只檢查了HTML標籤/樣式是否存在，沒有真正用Word開啟檢查實際分頁行為，這次補上這一步才抓到真正原因。
- **真正原因**：Word的HTML轉檔引擎不是照瀏覽器的CSS規則解讀`page-break-before`——把這個樣式寫在包住整篇文章的`<div>`上，Word會誤把這個換頁指令套用到div裡面**每一個**段落/標題/清單項目，造成「每個段落各自跳到新的一頁」，這正是使用者截圖看到的「一行字一頁」。
- **正確做法（沿用Word自己另存新檔時用的原生寫法）**：4篇文章改成各自獨立的`WordSection1~4`區塊，每個區塊有自己的`@page`版面設定；文章與文章之間的換頁改用Word專屬的特殊分行符號（`<br style="mso-special-character:line-break;page-break-before:always">`）觸發，不再把換頁樣式直接寫在文章的`<div>`上。
- **這次的驗證方式不一樣**：改用電腦本機真正安裝的 Microsoft Word（透過 PowerShell COM 自動化），實際開啟生成出來的檔案，逐段落讀取「這段文字在Word裡實際落在第幾頁」，確認：4篇文章分別從第1、7、10、14頁開始（每篇乾淨地從新頁面起頭），每一頁的實際字數都在合理範圍（2500~3600字元），沒有再出現空白頁或一行字一頁的狀況。這是比v1.22.4當時「只檢查HTML標籤存在」更嚴格、更真實的驗證方式。

## [v1.23.1] - 2026-08-13
### 調整＋修復（搜尋補充佐證：查詢數量改為彈性3-5個，修復標籤解析漏字bug）
- **查詢數量由固定2個改為彈性3-5個**：v1.23.0上線前討論定案的是「AI依角度複雜度自己決定2-3個」，但實作時為求簡化寫死成固定2個，未明確告知使用者這個簡化。經使用者確認後，改為讓AI依每篇文章角度是否值得多花一個查詢角度，自行決定要3、4還是5個查詢（例如購買指南型這種資料需求量大的角度可以用到5個，較單純的角度用3個即可）。
- **修復標籤解析bug**：實測時發現AI偶爾會把查詢標籤的結尾編號寫錯（例如第2個查詢的開頭標`QUERY_2`，結尾卻誤植成`QUERY_3`），導致原本嚴格配對開頭/結尾標籤編號的解析邏輯，會把這個查詢詞整個漏掉（不會出錯，但會少用一個查詢額度）。改為只認開頭標籤編號、抓到下一個標籤就切段的寬容解析方式，不再要求結尾標籤編號也要對得上。
- 已用真實API重新測試驗證：查詢數量确實依角度差異化（4/5/4/4，共17個查詢），修復後重跑一次未再出現標籤編號不一致的解析遺漏；生成的文章內文確認出現具體、可引用的外部市場數據（例如「Edge AI市場規模預計從2024年233億美元成長到2034年1966億美元，年複合成長率23.8%」這類原本產品頁面不會有的產業級數據，以自然改寫、不逐句照抄的方式呈現）。

## [v1.23.0] - 2026-08-13
### 新增（SEO/GEO文章生成：搜尋補充佐證，Tavily串接）
- **背景**：先前測試過Gemini原生的Google Search grounding功能（7次真實測試涵蓋產品/廣告/文章三種類型），確認grounding從未真正觸發搜尋，且新版模型（2.5/3.1/3.5系列）都無法強制模型一定要搜尋。改用「自己控制搜尋時機」的做法：拆解關鍵字→主動搜尋→把結果餵給AI生成。
- **範圍**：只套用在SEO/GEO文章生成的Step2（正式生成4篇文章），產品文案／廣告文案不動。
- **查詢詞怎麼來**：新增一次輕量AI呼叫，針對已選定的4篇文章（主文+3篇欉集），依各篇的內容角色（定義型/技術型/案例型/比較型/痛點型）產出對應角度的2個查詢詞——例如定義型找「產業標準定義＋市場概覽」，技術型找「規格效能數據＋業界評測評論」，案例型找「實務案例＋產業採用數據」，比較型找「評選標準＋市場比較資料」。
- **搜尋**：改用 Tavily（RAG導向搜尋API，直接回傳整理好的內容摘要，不需要像Google搜尋結果那樣再另外爬一次全文），透過新增的後端代理 `/api/tavily-search` 呼叫（金鑰存於Azure環境變數 `TAVILY_API_KEY`，前端不會接觸到金鑰本身），8個查詢平行送出。
- **生成時如何使用**：搜尋結果整理成「補充研究素材」餵進文章生成prompt，並明確要求AI：只挑2-4個最相關的事實、不得逐句照抄搜尋結果原文（版權考量，只能提煉事實後用品牌自己的語氣重寫）、引用時要自然帶出來源感（例如「根據近期產業數據顯示」）但不附外部連結、產品本身的真實資料永遠優先於外部搜尋結果、搜尋結果不夠相關時可以完全不用。
- **失敗容錯**：搜尋或查詢詞生成失敗時，該篇文章直接跳過搜尋補充繼續正常生成，不會讓整個流程卡住或報錯。
- 已用真實API完整測試驗證：8個查詢詞全部正確產出且語意對應各篇文章角色、Tavily真實回傳相關搜尋結果（非空結果）、4篇文章的生成prompt均確認內含研究素材區塊、最終生成的文章內文中實際出現依規範自然改寫、帶來源感、無逐句照抄的外部數據引用（例如「根據近期產業數據顯示，工業自動化應用占Edge AI推論伺服器全球應用營收26.8%」這類真實驗證到的句子）。

## [v1.22.6] - 2026-08-13
### 優化（生成按鈕按下後捲動到頁面最底部）
- **修正捲動目標**：按下藍色「執行 AI 內容生成」按鈕後，原本是捲到結果區塊最上方對齊視窗頂部，改為捲到整個頁面實際最底部，桌機、手機共用同一套邏輯（無需另外判斷裝置）。
- **修正捲動時機**：改為在 loading 動畫內容（`workerLoaderHtml`）真正塞入畫面後才量測頁面高度並捲動，避免高度算漏剛塞進去的內容。
- 已用真實測試驗證：捲動目標位置計算正確（精準對齊頁面最底部），文章與產品/廣告兩處呼叫點皆已套用；`behavior:'smooth'` 的滑順動畫本身受限於測試工具（背景分頁不產生畫面幀）無法視覺確認，已誠實告知使用者於正式站實際操作時順手確認。

## [v1.22.5] - 2026-08-13
### 改版（產品/廣告文案Excel匯出：欄寬、自動換行、標頭上色）
- **換用函式庫**：原本的 `xlsx.full.min.js`（SheetJS社群免費版）只能免費做到欄寬設定，儲存格上色與自動換行是付費Pro功能才支援。改用 `xlsx-js-style`（SheetJS社群版基礎上外掛基本樣式支援的第三方套件，GitHub: gitbrent/xlsx-js-style）取代，API相容不需改動其他呼叫方式。
- **欄寬**：依欄位語意分寬窄——說明性長文字欄位（Meta Description/TL;DR/GEO Features/Expert FAQ/Ads系列文案）給寬欄，型號等短欄位維持窄欄，網址欄位給中等寬度。
- **標頭列上色**：標頭列改為品牌藍底（#005BAC）＋白色粗體字，一眼分辨欄位。
- **內文自動換行**：所有資料儲存格加上自動換行＋靠上對齊，長文字不再被截斷或蓋住相鄰欄位。
- 已用真實API測試驗證：不只檢查記憶體中的樣式物件，還直接解壓縮實際下載的.xlsx檔案（ZIP格式）讀取原始XML，逐一確認欄寬、標頭底色、粗體字、自動換行都真的寫進了檔案本身，不是只存在瀏覽器記憶體裡沒真正輸出。

## [v1.22.4] - 2026-08-13
### 修復（SEO/GEO文章匯出Word排版跑掉，尤其後續頁面）
- **補上Word專用樣式表與版面設定**：匯出的.doc檔（本質為HTML）先前完全沒有`<style>`區塊，標題/內文用的是網頁排版常見的class寫法，但Word完全不認得這種寫法，導致所有標題、內文在Word裡長相一模一樣、毫無層次。新增正式的`<style>`區塊，包含Word官方要求的`@page`/`mso-`版面設定，讓h1/h2/h3/p/ul/li都有Word看得懂的真實樣式定義。
- **修正分頁方式**：原本每篇文章間只用單純的換頁指令，缺少Word正式版面設定搭配，容易在換頁處造成間距錯亂（尤其第2篇以後）。改為用單一`div.WordSection1`包住全文並掛上`@page`設定，讓文章間的換頁乾淨可靠。
- **新增條列清單轉換**：`simpleMarkdownToHtml()`先前只處理標題與粗體，遇到`- item`或`1. item`這類條列markdown會直接當純文字包進段落，顯示成一堆帶符號的文字。新增偵測邏輯，正確轉換成`<ul>/<ol><li>`真正的清單。
- 已用真實API（iotmart MIC-742-AT7A1）跑完整4篇文章生成＋匯出流程驗證：擷取實際下載的Word HTML內容確認`<style>`與`@page`區塊存在、所有標題標籤（h1/h2/h3）正確配對無殘留、無格式跑掉的標籤；另以獨立單元測試確認條列/編號清單能正確轉換成`<ul>/<ol>`。

## [v1.22.3] - 2026-08-12
### 改版（產品文案卡片：移除底部複製標籤按鈕，併入頂部複製全部）
- **移除底部「複製標籤」按鈕**：底部只保留標籤/分類的純文字顯示，不再是獨立可複製區塊。
- **頂部「複製全部」按鈕擴大複製範圍**：新增標題+說明+特色+TL;DR+FAQ 之外，尾端補上標籤與分類，一次複製涵蓋卡片全部內容。
- 清除因此不再使用的 `copyMeta` 5語言i18n字串。
- 已用真實API測試驗證：底部按鈕確認移除、複製全部按鈕內容結尾正確包含Tags/Category、標籤文字顯示不受影響。

## [v1.22.2] - 2026-08-12
### 改版（意見反饋視窗比照 AI Content Center 風格與流程）
- **外觀改版**：圖示由黃色燈泡改為淺綠底＋綠色對話框icon；按鈕配色由黃色改為本站品牌藍（#005bac）。
- **新增「你的名字」欄位**：已登入時自動帶入登入信箱作為預設值，使用者可自行編輯覆蓋；未登入則留空手動填寫。
- **送出後行為改版**：不再是跳toast提示＋自動關閉視窗，改為在視窗內顯示「✅ 感謝你的反饋！」感謝畫面，使用者需自行按「關閉」離開；驗證錯誤（未填寫）也改為視窗內inline紅字提示，取代原本的toast。
- Firestore寫入欄位新增 `name`（沿用既有 `email` 欄位供後台管理儀表板追蹤，不受影響）。
- 已本機測試：驗證錯誤正確顯示、感謝畫面文案與樣式正確、關閉按鈕正常運作、按鈕配色正確為品牌藍。

## [v1.22.1] - 2026-08-12
### 修復（手機版匯出按鈕左邊界對齊、字數限制prompt優化）
- **修正手機版藍色生成按鈕與綠色匯出按鈕左邊界未對齊**：沿用桌機右邊界對齊的同一套即時量測機制，手機堆疊排版時改為對齊左邊界（兩者原本因為所在卡片的padding不同，天生會有17px落差）。已實測手機（412px）左邊界誤差0px，桌機右邊界誤差0px維持正常，未受影響。
- **優化產品文案/廣告文案的字數限制prompt**：字數指令改為「目標區間+硬上限」並留安全邊際（例如描述目標60-68字、硬上限75字，不再指示AI貼著上限寫），新增「字數限制優先於內容完整性」取捨規則（衝突時寧可縮短CTA也不超標），CTA範例改為要求逐字複用、禁止自行加長敬語變化。
- **誠實記錄實測結果**：真實API測試兩輪，字數超標比例從約35%降到10%左右，但**未能完全消除**——這是LLM對中日韓文字計數天生不可靠的已知限制，光靠prompt措辭優化無法保證100%合規，僅能降低超標機率與幅度。如需真正保證合規，需額外建置「生成後自動偵測超標→自動重寫」機制（本次未實作，經使用者確認範圍僅做prompt優化）。

## [v1.22.0] - 2026-08-12
### 修正＋新增（目標優化引擎：依實證研究修正客製化邏輯，並延伸進SEO/GEO文章生成）
- **依2026 GEO/AEO實證研究重新校正9種引擎的客製化指令**：先前「高密度數據」只設定給Perplexity專屬，但Princeton/Georgia Tech GEO論文（KDD'24）與多份具名研究（Ahrefs/Profound/AIVO）顯示「加入具體數據/來源/引言」對**所有引擎**均有30-40%引用率提升，屬通用加分項，非Perplexity獨有。Perplexity真正的差異化特徵改為「開頭100字內即給出完整答案」＋「高度重視內容新鮮度」。ChatGPT依研究強化為「比較/清單情境優先用條列格式」（比較意圖查詢的listicit引用率達100%）。Gemini／Claude／DeepSeek／中國四引擎依對應研究微調用詞，方向與原設計大致吻合但用語更精準。
- **修正產品文案9套引擎邏輯的程式結構**：抽成共用函式 `getEngineInstructions(geo)`，取代原本重複寫在 `if/else` 裡的邏輯，同一套定義同時供產品文案與文章生成共用，避免未來修改時兩處各改各的。
- **新增：SEO/GEO文章生成（Step2逐篇正式生成）首次接入目標優化引擎邏輯**：`articleWorkflowState.geo` 過去只是存起來從未被讀取，文章語氣完全不受這個選項影響；現在 `buildArticleStep2Prompt()` 會依所選引擎插入對應語氣指令（如通義千問→強調供應鏈/客製化角度、DeepSeek→先講推理鏈再下結論、Perplexity→段落開頭即給答案）。
- **新增防呆限定句**：通義千問／文心一言／豆包等指令，明確加上「僅在資料來源真的提及具體數字（MOQ、報價、認證編號等）時才引用，若無則用質性描述」，避免AI為了配合語氣風格而編造未經證實的具體數字。
- 已用真實Gemini API實測驗證：SEO/GEO文章＋通義千問組合，生成內文確實出現供應鏈/OEM/ODM/客製化語氣描述，且未出現任何編造的具體數字；產品文案＋Gemini組合，說明開頭維持研究要求的「定義性直接答案」風格，確認重構未破壞既有功能。

## [v1.21.11] - 2026-08-12
### 修復（生成/匯出/確認按鈕：真正的右邊界對齊 + 尺寸統一）
- **修正桌機版藍色生成按鈕與綠色匯出按鈕右邊界仍未對齊**：先前用CSS margin/padding硬算兩個容器的內縮差異，一直有新的誤差冒出來。改為用JavaScript在每次顯示綠色按鈕時，直接量測藍色按鈕實際渲染出來的寬度與右邊界，動態設定綠色按鈕的寬度與位移（`position:relative; right:Npx`），不再依賴猜測CSS數值。桌機同排時精準對齊右邊界，手機堆疊排版時只對齊寬度（不套用位移，避免誤判）。
- **新增文章流程「確認並生成4篇文章」按鈕的尺寸同步**：套用同一套動態量測機制（抽成共用函式 `syncWidthToSubmitBtn`），讓這顆按鈕的寬高也跟藍色生成按鈕一致。
- 已排除本機測試工具本身的CSS transition凍結假象（無頭分頁背景狀態導致的既有已知限制），確認為真正生效的量測結果：桌機三顆按鈕（生成/匯出/確認）寬256px、高48px完全一致，右邊界對齊誤差0px；手機三顆按鈕寬度皆與各自情境下的生成按鈕寬度一致。

## [v1.21.10] - 2026-08-12
### 修復（生成中進度條寬度、匯出按鈕全站排版）
- **修正桌機版藍色生成按鈕加寬後擠壞4個下拉選單排版**：v1.21.9 為避免文字換行把按鈕加寬到288px，但擠佔了下拉選單區的可用寬度（4個選單需724px，被擠到只剩719px，第4個選單被迫換行）。改為264px（實測5語言最長字串仍不換行的最小安全值），4個選單重新排回同一排。
- **修正產出卡片「標籤＋位元數」排版在手機窄螢幕擠成3行、左右不對齊**：META TITLE／META DESCRIPTION／TL;DR 三處的標籤晶片與位元數晶片原本用 `justify-between items-end`（擠不下時硬擠成多行、底部對齊造成高度落差明顯歪斜），改為 `flex-wrap items-start`（擠不下時換行成兩排、各自維持單行），桌機寬螢幕不受影響仍維持同排。
- **修正匯出按鈕在手機版寬度跟生成按鈕對不齊**：綠色匯出按鈕所在區塊比藍色生成按鈕的卡片少一層內縮padding，導致手機版兩者寬度有32px落差。補上對應邊距（`calc(100%-2rem)` + 左右各1rem留白），手機版兩鈕寬度誤差收斂到1px內。

## [v1.21.9] - 2026-08-12
### 修復（生成/匯出按鈕：桌機換行問題、手機寬度與對齊）
- **修正桌機版藍色生成按鈕英文換行成兩行、跟綠色匯出按鈕不一樣高**：上一版固定寬度 240px 未實測所有語言最長字串，日文版最長需 244px 才能維持一行，導致英文「Execute AI Generation」在 240px 換成兩行、按鈕變高。改為 288px 並鎖定不換行，已實測 5 語言皆維持單行、兩鈕同高（48px）。
- **修正手機版兩顆按鈕比旁邊區塊窄、靠左不置中**：改為滿版寬，各自對齊自己所屬區塊（藍色對齊控制面板下拉選單、綠色對齊結果內容區），滿版本身即自然置中，不需額外置中設定。
- 已對全站所有按鈕做一輪盤點與手機窄螢幕（375px）溢出檢查，確認「確認並生成4篇文章」按鈕（最長英文版29字）、比較表選取按鈕等皆無爆版問題。

## [v1.21.8] - 2026-08-12
### 修復（生成中進度百分比配色、匯出按鈕位置與寬度微調）
- **生成中進度百分比文字改為與進度條同色**：原本是灰色（`text-slate-400`），與藍色進度條視覺脫節，改為同色系藍色（`#005bac`）並加粗。
- **匯出按鈕位置調整回「優化產出結果」標題旁**：上一版曾嘗試移到藍色生成按鈕旁邊營造主從關係，但使用者回饋位置應維持貼近結果區塊標題（跟輸出內容在同一個視覺群組），主從關係改用樣式（藍色實心＝主要動作、綠色外框＝次要動作）表達，不靠物理相鄰。
- **兩顆按鈕改為固定同寬（240px）**：桌機、手機皆套用同一寬度數值，視覺上明確呈現兩者為一組相關動作。

## [v1.21.7] - 2026-08-12
### 新增（生成中畫面改版：像素風工人動畫 + 真實進度條）
- **3處「生成中」大畫面的轉圈動畫，全部換成像素風工人拼命寫字動畫**：SEO/GEO文章主題規劃（Step1）、逐篇生成（Step2，4篇文章）、產品文案/廣告文案生成，統一改用新增的 `worker-writing-pixel.gif`（240×240、40×40像素網格、4幀循環，已去背），取代原本的 `fa-circle-notch fa-spin` 轉圈圖示。
- **新增真實進度條百分比**：逐篇生成畫面（Step2）採用「真實斷點＋等待中模擬爬升」混合邏輯——每篇文章開始生成時，進度條從該篇的真實起始百分比緩慢爬升，最高爬到該篇區間的88%就卡住等待，該篇真正完成時立即跳到真實的完成百分比，避免長時間無回應時進度條看起來卡死。另外兩處單次AI呼叫、沒有真實中間進度可用的畫面（Step1主題規劃、產品/廣告文案生成），採用同一套邏輯緩慢爬到92%卡住、完成瞬間跳100%。
- 新增共用函式 `workerLoaderHtml()`、`startFakeProgress()`、`finishFakeProgress()`，3處生成中畫面共用同一套版型與進度條邏輯，不必各自維護。
- 已用真實Gemini API本機端到端測試：文章Step1、Step2四篇逐篇生成（含真實斷點正確觸發）、產品文案生成，三條路徑皆確認正常運作。

### 修復（匯出按鈕：重置時機與主從關係）
- **修正「按下生成後，舊的匯出按鈕殘留」問題**：綠色匯出按鈕（Excel/Word）過去只在生成完成後才更新狀態，按下藍色「執行AI內容生成」開始新一輪生成時完全沒有處理，導致整個生成過程中畫面上留著上一輪生成的舊按鈕（模式、文字都對不上新內容）。改為按下生成按鈕的瞬間立即隱藏舊按鈕，確認新一輪生成完成後才重新顯示對應模式的按鈕。
- **調整匯出按鈕位置與視覺層級**：原本獨立放在「優化產出結果」標題右側，跟藍色生成按鈕距離遠、視覺重量也一樣重（都是實心色塊），容易讓人誤解兩者無關。改為緊貼在藍色生成按鈕右側、同高同排，並改用白底＋綠色邊框的外框樣式（非實心），視覺上明確呈現「藍色是主要動作、綠色是依附於它的次要動作」的主從關係。

## [v1.21.6] - 2026-08-11
### 修復（版面微調：文章結果頁籤手機排版、比較表選取列過高）
- **文章結果頁籤手機版排版錯亂**：固定 4 顆頁籤（主文＋3篇欉集）原本用 `flex flex-wrap` 自動換行，手機窄螢幕會擠成「3顆一排＋1顆單獨一排」的不平衡排版。改為手機寬度固定 `grid-cols-2` 2×2 格子排版，桌面維持原本 `flex flex-wrap` 一排展開；頁籤文字加上置中與截斷，避免長標籤撐破格子。
- **SEO/GEO 文章主題比較表「組合1/2/3」選取列被拉成跟內容列一樣高的空白大方塊**：根因是比較表格容器設定 `sm:grid-rows-5` 強制 5 列等高（依最高列拉齊），導致文字量少的選取列被拉出約 211px 的大片空白。移除該設定，改為每列依自身內容各自撐高；同時將選取列文字放大（約 14px → 24-30px），提升可讀性與點擊辨識度。已本機用假資料量測 DOM 尺寸驗證：桌機選取列高度由 211px 降至 56px，手機頁籤排列由 3+1 改為工整 2+2。

## [v1.21.5] - 2026-08-11
### 修復（多語系覆蓋全面稽核收尾）
- **稽核「頂部選單重構＋SEO/GEO文章功能」新增的所有選項與標籤在 5 種語言下的覆蓋狀況**：逐一核對 RBU 選單、生成內容選單、生成策略選單、8 種文章角度標籤、以及全部 `article*` 前綴的 96 個 i18n key，確認 5 語言字典完全對齊、無缺漏、無跨語言誤植。RBU 代碼（ATW/ACN 等）與中國 AI 引擎專有名詞（文心一言／通義千問／DeepSeek／豆包）維持不翻譯，屬合理設計，非缺漏。
- **修正文章 Step 2 生成失敗時顯示寫死英文訊息**：`window.confirmArticleGeneration` 內 `if (!outputText) throw new Error('Empty response')` 不論介面語言為何都固定顯示英文，改為新增 `articleStep2Failed` i18n key（5 語言皆補齊），與既有 Step 1 失敗訊息（`articleStep1Failed`）採相同處理模式。

## [v1.21.4] - 2026-08-11
### 改善（SEO/GEO 文章主題比較表 4 項微調）
- **選擇理由直接顯示，不再藏進滑鼠提示圖示**：v1.21.2 為了縮短卡片高度，把理由文字收進了滑鼠移過去才顯示的提示圖示，使用者回饋「說明都不見了」。改回在標題下方直接顯示完整理由文字。
- **主文／欉集文一眼可辨**：主文欄位加上琥珀色底色＋白字「PILLAR」徽章、欉集文欄位維持白底＋灰字「CLUSTER」徽章，桌面比較表與手機堆疊版皆套用同一套視覺規則。
- **選取的整套組合改用底色標示**：原本僅有邊框顏色變化不夠明顯，`window.selectArticleBundle()` 改為同時切換底色（`bg-blue-100`）、邊框與外框強調（`ring-2`），並保留角色底色作為未選取狀態的預設值。
- **新增文章匯出 Word 功能，並與既有「匯出 Excel 報告」按鈕整合**：SEO/GEO 文章模式下，4 篇文章生成完成後，原本的匯出按鈕自動切換為「匯出 Word」（`window.exportArticlesToWord`），依序輸出標題、META 描述、Key Takeaways、內文（自動將網頁視覺用的標題階層轉換為 Word 文件應有的 H1/H2/H3 正式階層）、FAQ 問答、標籤與分類，每篇文章間以分頁符號隔開；產品文案／廣告文案模式則維持原本的「匯出 Excel 報告」（`window.exportToExcel`）。新增 `window.setExportMode()` 統一管理按鈕圖示、文字與點擊行為的切換。
- 已用模擬完整文章資料（繞過 Gemini API 呼叫）驗證匯出的 Word 檔案內容與標題階層轉換邏輯正確無誤。

## [v1.21.3] - 2026-08-11
### 修復（全站 RWD 手機版總體檢）
- **移除 4 處故障的漢堡選單按鈕（核心問題）**：這些按鈕呼叫 `document.getElementById('mobile-menu')`，但 `#mobile-menu` 這個元素從未存在於頁面中，點擊完全無作用。其中兩處是複製貼上錯誤，混進了不該出現的地方：**每一行廣告文案結果**（`renderAdsItems`）與**產品文案卡片標題**都各夾帶一顆無用按鈕，手機上每次生成內容都會看到大量無效按鈕，這是「手機版慘不忍睹」的主要成因。導覽列本身也有重複的一顆，一併移除。
- **導覽列手機版全面壓縮**：登入／意見反饋按鈕在手機寬度改為只顯示圖示、隱藏文字；連線狀態指示改為只顯示圖示/色點；標題文字加上 `truncate` 防止換行破版。原本 5 個項目擠成一排寬達 504px（在僅 320px 的手機螢幕上，多顆按鈕被推到畫面外看不到、點不到），現已完全收進版面內，零溢出。
- **SEO/GEO 文章主題比較表：修正手機版堆疊順序錯亂**：改版後桌面雖排成表格，但手機版（單欄堆疊）DOM 順序仍是「橫向逐列」，導致 3 套組合的內容交錯混雜（所有主文疊在一起、所有欉集1疊在一起），無法一次讀完一套組合。改為 DOM 依「一套接一套」自然排列，並用 CSS Grid 的 `col-start`/`row-start` 讓桌面版重新定位成比較表格，手機與桌面共用同一份 DOM，不必維護兩份結構。
- **管理員儀表板表格加上橫向捲動容器**：6 欄的使用者追蹤表格在手機寬度（最小 640px 內容 vs 296px 彈窗）原本會直接撐爆彈窗版面，現改為 `overflow-x-auto` 包裹，可在彈窗內左右滑動查看完整欄位。

已用 320px 寬度實測全頁面（首頁、導覽列、管理儀表板、登入/意見反饋彈窗、SEO/GEO 文章比較表）掃描零版面溢出。版號升至 v1.21.3。

## [v1.21.2] - 2026-08-11
### 修復
- **SEO/GEO 文章主題選擇畫面改為比較表格排版**：原本「一張卡片＝一整套組合」的設計，為了不截斷長標題把 4 個標題疊在同一張卡片裡，導致卡片被撐得過高，破壞了三套並排比較的可讀性。改為橫軸＝3 套組合、縱軸＝角色（主文／欉集1-3）的比較表格：同一列只需容納「同角色 3 個標題裡最長的那個」，不再是 4 個標題疊加的總高度。標題文字保留完整不截斷（改用可自動換行的可編輯區塊取代單行輸入框），理由文字收起來改成滑鼠移過去顯示的提示圖示，選取整套組合改為點擊該欄任一格或頂部按鈕。已本機驗證：標題不截斷、選取狀態正確反映在整欄、讀取編輯後標題的邏輯不受影響。

## [v1.21.1] - 2026-08-11
### 變更
- **導覽列左上角換成研華正式 Logo**：移除原本的藍色方塊＋晶片圖示佔位符，改用使用者提供的 `advantech-logo.png`（新增於專案根目錄，隨 `express.static` 一併部署）。桌面高度 40px、手機高度 32px，等比例縮放不變形。已本機驗證兩種寬度下顯示正常、無版面溢出。

## [v1.21.0] - 2026-08-11
### 新增
- **新增日文、韓文介面語言，語言選單順序改為 英文＞日文＞韓文＞繁中＞簡中**：
  - 品牌名稱經官網查證：日文用「アドバンテック」（片假名），韓文用「어드밴텍」，非自行猜測。
  - 新增語言各補齊全部 42 個既有介面字串，並涵蓋 SEO/GEO 文章功能的 8 種角度標籤。
  - AI 生成內容語言邏輯（`targetLangLabel`/`brandName`/`grammarRule`/字元計數規則）擴充支援日文、韓文，廣告文案的全形逗號規則區分中文（，）與日文（、）。
  - 本機實測：日文、韓文介面切換、廣告文案生成、SEO/GEO 文章 Step 1 主題規劃，皆確認為道地語言輸出，非機器直譯痕跡。
### 修復
- **修正散落在程式碼各處、未走 i18n 的寫死中文字串**：包含網址爬取進度提示、圖片讀取失敗訊息、檔案解析失敗標題、管理儀表板讀取/清空提示等共 15 處，改為統一透過 i18n 字典管理，這些訊息過去無論介面語言為何都固定顯示繁體中文。
- **修正 zh-TW 字典內一處簡繁混用錯字**：「目前無任何建议」的「议」誤植為簡體字，已修正為「議」。

## [v1.20.0] - 2026-08-11
### 新增
- **SEO/GEO 文章生成：主文＋欉集叢集架構（Topic Cluster Model）**：
  - 選「生成內容→SEO/GEO文章」後，兩階段生成流程：
    - **Step 1（主題規劃）**：AI 從輸入內容萃取事實，對 8 種內容角度（選購指南／痛點解決／定義權威／產業應用／應用場景／技術深挖／比較決策／FAQ大全）評分，產出 3 套「主文＋3篇欉集」組合。主文候選 1 個保底（4 廣角度中最高分）＋2 個公開競爭（剩餘 7 種角度中最高分），欉集則從剩餘角度選 3 個不重複的。「生成策略」選單同步影響評分權重（購買意圖優先／品牌聲量優先分別加權對應角度）。
    - **使用者確認**：3 套組合的所有標題皆可點擊編輯，確認後才進入正式生成。
    - **Step 2（正式生成）**：逐篇生成 4 篇完整文章（顯示進度 X/4），每篇依 2026 GEO/AEO 研究的結構規則撰寫（前 30% 內容前置關鍵事實、Key Takeaways 條列、H2/H3 標題階層、段落 3-4 句＋粗體重點、5 組獨立成立的 FAQ），且互相在正文中提及其他 3 篇標題（不生成假連結）。
  - 前端自動從文章 H2 標題產生目錄（TOC），FAQ 採用可直接對應 FAQPage 結構化資料的格式。
  - 已本機實測完整流程：Step 1 主題規劃、標題編輯、Step 2 四篇文章生成、標籤切換、跨文章交叉引用（4 篇皆正確提及另外 3 篇的確切標題）。

## [v1.19.6] - 2026-08-11
### 優化
- **選單與送出按鈕合併回同一行，並依內容給合理寬度**：v1.19.5 讓四個選單等寬雖然排列整齊，但等寬導致文字短的「RBU」浪費空間、按鈕被迫獨立一行留下大片空白。改為依實際文字長度給寬度（RBU 最窄、生成策略最寬），寬螢幕時選單與送出按鈕合併回同一行靠右對齊；手機寬度維持 2×2 排列＋按鈕獨立一行。已用瀏覽器實測 1230px 與手機寬度座標，確認一行排列與 2×2 排列皆正確無重疊。

## [v1.19.5] - 2026-08-11
### 修復
- **修正頂部四選單排版錯亂**：v1.19.4 新增 RBU／生成內容兩個選單後，四個選單改用各自固定寬度（44/52/56/64）搭配 `flex-wrap`，在中等寬度下擠成「三個一排＋一個孤兒」，按鈕又因 `justify-between` 被推到右側留下大片空白。改為 `grid grid-cols-2 lg:grid-cols-4`，四個選單等寬對齊：桌面寬度一行四欄，手機寬度 2×2 整齊排列；送出按鈕移到選單下方獨立一行、靠右對齊。已用瀏覽器實測 1280px 與手機寬度下的座標，確認排列整齊無錯位。

## [v1.19.4] - 2026-08-11
### 變更
- **頂部選單重構為 RBU＞生成內容＞目標優化引擎＞生成策略**：
  - 新增「RBU」下拉選單：ANA/AEU/ACN/ATW/AJP/AKR/AIntercom/AIN/AAU/Others，預設 ANA。
  - 新增「生成內容」下拉選單：網站產品文案（預設）／廣告文案／SEO-GEO文章。
  - 原「目標優化引擎」中的 Google Ads 移除，改由選擇「生成內容→廣告文案」觸發同一套廣告文案生成邏輯（`currentEngineMode` 改依生成內容判斷，不再依目標優化引擎）。
  - 選 RBU=ACN 時，「目標優化引擎」自動換成中國主流 AI（文心一言／通義千問／DeepSeek／豆包），並各自比照 Gemini/Claude/ChatGPT/Perplexity 的既有模式，給予客製化 FAQ 語氣風格；切回其他 RBU 還原預設引擎清單。
  - 「標題生成策略」改名為「生成策略」並移至選單最後一位。
  - 新增「SEO/GEO文章」選項的切換與提示邏輯（顯示「規劃中」訊息，不觸發任何 API 呼叫），實際生成規格待下階段設計。
  - en/zh-TW/zh-CN 三語系皆已補齊對應翻譯。

## [v1.19.3] - 2026-08-11
### 新增
- **管理員儀表板改為記錄實際使用頻率**（[index.html:449-476](index.html:449)）：
  - 新增 `window.trackUsage()`，每次 AI 生成成功後觸發，寫入 Firestore `user_stats` 文件的 `generationCount`（生成次數累加）與 `lastUsedAt`（最後使用時間）。
  - 原因：原本「最後登入時間」只在使用者手動重新輸入信箱登入時才更新，但瀏覽器會快取信箱、之後自動略過登入畫面，導致長期不動、被誤判為「儀表板壞掉」。實測 Firebase 讀寫與儀表板渲染皆正常，純粹是統計口徑跟 README 描述的「使用頻率」不一致。
  - 儀表板新增「生成次數」「最後使用時間」兩欄，並改為依「最後使用時間」排序（無使用紀錄則退回登入時間排序），舊帳號無新欄位資料時顯示「0 次／尚未使用」。

## [v1.19.2] - 2026-08-10
### 變更
- **爬蟲代理引擎由 Jina Reader 改為 Apify Website Content Crawler**（[server.js:19-53](server.js:19)）：
  - 原因：Jina Reader 對有反爬蟲防護的網站（例如 iotmart.com，Salesforce Experience Cloud 商店頁）完全抓不到內容，只回傳網站攔截畫面，AI 因無真實資料而輸出大量 N/A。
  - 改用 Apify `apify/website-content-crawler` actor，設定 `crawlerType: playwright:firefox`、`dynamicContentWaitSecs: 20`、`htmlTransformer: none`，關閉預設的 Readability 式自動內容判斷（該演算法會誤判把賣點/認證/價格區塊當雜訊丟棄），改用手動 `removeElementsCssSelector` 排除常見同意管理工具（CookieYes/OneTrust/Cookiebot/TrustArc）與導覽/頁尾。
  - 需在 Azure 後台額外設定環境變數 `APIFY_API_KEY`。
  - 已知取捨：Apify 為用量計費（非固定免費額度），且抓取耗時較長（單次約 25-60 秒，逾時上限調整為 140 秒）；`removeElementsCssSelector` 為手動維護清單，遇到用其他同意管理工具的網站可能仍有殘留雜訊，需個別調整。

## [v1.19.1] - 2026-08-10
### 修復
- **產品型號／關鍵字幻覺修正 (commit `ca98b17`)**：
  - 目標網址與參考網址欄位改為逐行 `trim()`，避免多餘空白造成判讀錯誤。
  - 輸入若非 `http` 開頭，判定為「產品型號或關鍵字」，直接跳過爬蟲並改以提示語引導 AI 使用內建知識庫，解決過去因抓取失敗而輸出 `N/A` 的問題。
  - 爬取失敗時的提示語改為引導 AI 以內建知識庫為主，不再插入干擾性的失敗標記。
- **編碼修復 (commit `299092e`)**：修正 `index.html` 中文字元毀損問題，同時保留 8 月的排版與版號更新。

### 維護
- **版號全面對齊**：`package.json` 自 `1.18.0` 補正至 `1.19.1`，與 `index.html`、`README.md` 一致。
- **文件校正**：README 部署說明更正為實際使用的 Azure Zip Deploy（OneDeploy），原「GitHub 自動觸發部署」敘述與 Azure 實際設定不符（`deployment source` 未綁定 GitHub，專案亦無 GitHub Actions）。
- **`.gitignore` 強化**：排除 `appdeploy.zip`、`publish_profiles.json` 與除錯用暫存檔，避免部署憑證與比對檔誤入版控。

## [v1.19.0] - 2026-08-03
### 新增與優化
- **行動版排版全面優化 (Mobile UI Overhaul)**：
  - 新增 mobile CSS `@media` 規則，解決手機版區塊擁擠與溢出問題。
  - Navbar 右側按鈕改為 Grid 排列，並將連線狀態指示器移至 Logo 右方節省空間。
  - 各 Section 的 padding 在小螢幕下縮減為 `p-4`，以增加內容可視範圍。
  - Textarea 與拖放上傳區塊的高度在手機版縮減。
  - 引擎與策略下拉選單在手機版改為全寬垂直排列。
  - 結果區塊文字與間距縮小，提升行動端閱讀體驗。
- **預設語系調整**：網頁載入時的預設語言由繁體中文 (`zh-TW`) 變更為英文 (`en`)。
- **基礎設施**：新增 `.deployment` 設定檔與 `.vscode/settings.json` 並納入版控，強化 Azure 部署穩定性。

## [v1.18.3] - 2026-06-18
### 新增
- **網頁抓取代理 (Scraping Proxy)**：
  - 後端新增 `/api/scrape` 路由。
  - 整合 Jina Reader API 將網頁轉換為 Markdown。
  - 實作 20 秒 Timeout 控制與錯誤攔截機制，成功避開前端 CORS 限制。

---
*備註：本專案遵循 Global Developer Constitution，自 v1.19.0 起嚴格執行每次修改自動更新版號與文件的規範。*
